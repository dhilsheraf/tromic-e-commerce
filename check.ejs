<script>
    (function ($) {
      "use strict";
  
      /* Sale statistics Chart */
        if ($('#myChart').length) {
            var ctx = document.getElementById('myChart').getContext('2d');
            var chart = new Chart(ctx, {
                type: 'line',
                data: {
                    labels: <%= JSON.stringify(months) %>,  // Dynamic month data
                    datasets: [{
                        label: 'Sales',
                        tension: 0.3,
                        fill: true,
                        backgroundColor: 'rgba(44, 120, 220, 0.2)',
                        borderColor: 'rgba(44, 120, 220)',
                        data: <%= JSON.stringify(revenues) %>  // Dynamic revenue data
                    }]
                },
                options: {
                    plugins: {
                        legend: {
                            labels: {
                                usePointStyle: true,
                            }
                        }
                    }
                }
            });
        }
  
      // Other charts...
  
  })(jQuery);
  </script>
  