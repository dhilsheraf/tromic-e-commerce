<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

</head>
<body class="bg-gradient-to-br  min-h-screen flex items-center justify-center p-4">
    <div class="w-full max-w-md bg-white shadow-2xl rounded-2xl overflow-hidden bg-pattern">
        <div class="bg-gradient-to-r from-red-500 to-black p-6 text-center">
            <i class="fas fa-lock text-white text-5xl mb-4 block"></i>
            <h2 class="text-3xl font-bold text-white">Forgot Password</h2>
        </div>
        
        <form action="/forgot-password" method="POST" class="p-8 space-y-6">
            <div class="space-y-2">
                <label class="block text-gray-700 font-semibold">Email Address</label>
                <div class="relative">
                    <i class="fas fa-envelope absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
                    <input 
                        type="email" 
                        name="email" 
                        required 
                        placeholder="Enter your email" 
                        class="w-full pl-10 pr-4 py-3 border-2 border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-300"
                    />
                </div>
            </div>
            
            <button 
                type="submit" 
                class="w-full bg-gradient-to-r from-black to-black text-white py-3 rounded-lg hover:opacity-90 transition duration-300 transform hover:scale-105 shadow-lg"
            >
                Reset Password
            </button>
            
            <div class="text-center">
                <a href="/login" class="text-red hover:underline flex items-center justify-center ">
                    <i class="fas fa-arrow-left mr-2"></i>
                    Back to Login
                </a>
            </div>
        </form>
        
        <div class="bg-gray-100 p-4 text-center text-gray-500 text-sm">
            © 2024 Secure Access. All rights reserved.
        </div>
    </div>
</body>
<script>
        // Check URL for status query parameter
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.has('status') && urlParams.get('status') === 'sent') {
            alert("If this email exists, a reset link has been sent.");

            setTimeout(() => {
                window.location.href = '/'
            }, 1000);
        }
</script>
</html>